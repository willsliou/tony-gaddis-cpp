#include <iostream>
using namespace std;

int main() {

	const float total_state,
		total_county, 
		state_tax = 0.04, 
		county_tax = 0.02;
	double purchase;
	total_state = purchase * state;
	total_county = purchase * county;
	cout << "The total state tax on a $95 purchase is: " << total_state;
	cout << "The total county tax on a $95 purchase is: " << total_county;
}