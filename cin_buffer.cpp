#include <iostream>       // for cin and cout
#include <iomanip>        // for outpupt formatting
#include <cmath>          // for advanced math such as pow(a, b), sin, cos, etc.
#include <ctime>          // for generating random numbers using the system time as the seed
#include <cstdlib>        // for advanced functions such as rand() for random numbers, exit function
#include <fstream>        // for external file operations
#include <string>         // for working with variables/arrays of type string
#include <cctype>         // for C-string operations
#include <random>         // for functions with random numbers
#include <vector>         // for vector operations

using std::cout;
using std::cin;
using std::endl;
using std::string;


int main() {
    // int myInt, myInt2;
    // string store;
    // cout << "Enter an int: "; // this must be before the cin.ignore()
    // cin.ignore();
    // cin >> myInt; // buffer is here
    // // cin >> myInt2;
    // cin >> store;
    // cout << "Please enter string to store: ";
    // getline(cin, store);
    // cout << store << endl;

        char myChar;
        cout << "Input: ";
        cin >> myChar;
        cout << myChar;

}